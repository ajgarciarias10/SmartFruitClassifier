# 🔧 Smart Fruit Classifier - Troubleshooting Guide

## ❌ Проблема: TensorFlow/NumPy совместимость

**Симптом:**

```
AttributeError: _ARRAY_API not found
ImportError: numpy.core._multiarray_umath failed to import
```

**Причина:** NumPy 2.x несовместим с вашей версией TensorFlow

**✅ Решение:**

```bash
# Вариант 1 (Рекомендуется): Использовать requirements.txt
pip install -r requirements.txt

# Вариант 2: Ручная установка совместимых версий
pip uninstall numpy tensorflow
pip install "numpy<2.0.0" "tensorflow>=2.15.0,<2.17.0"

# Вариант 3: Если ничего не помогает
pip install "numpy==1.24.3" "tensorflow==2.15.0"
```

## 📊 Проблема: Недостаточно данных для обучения

**Текущий статус датасета:**

- ✅ Apple: достаточно данных
- ⚠️ Banana, Cucumber, Grapefruit: мало данных
- ❌ Pomegranate: очень мало данных
- ❌ Validation/Test наборы: критически мало данных

**✅ Решения:**

### 1. Скачать больше данных (требует FiftyOne):

```bash
pip install fiftyone
python loadFiftyOne.py
```

### 2. Использовать Data Augmentation (уже включена в код):

- Поворот изображений
- Сдвиги и масштабирование
- Горизонтальные отражения

### 3. Уменьшить количество классов:

Временно обучаться только на классах с достаточными данными (Apple, Banana)

## 🚀 Пошаговый план запуска:

### Шаг 1: Проверка системы

```bash
python check_dependencies.py
```

### Шаг 2: Исправление зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 3: Проверка датасета

```bash
python checkHowManyFruits.py
```

### Шаг 4: Запуск обучения

```bash
python main.py
```

## 🔍 Дополнительные проверки:

### Проверка GPU поддержки:

```python
import tensorflow as tf
print("Доступные GPU:", tf.config.list_physical_devices('GPU'))
```

### Проверка загруженной модели:

```python
from FruitDetector import FruitDetector
detector = FruitDetector(224, 5)
detector.load_model('fruit_classifier_apple_banana_avocado.h5')
```

## 📋 Чеклист перед обучением:

- [ ] ✅ Python 3.8+ установлен
- [ ] ✅ TensorFlow работает без ошибок
- [ ] ✅ Папки dataset/train, dataset/val, dataset/test существуют
- [ ] ✅ В каждой папке есть подпапка Fruit/ с классами
- [ ] ✅ Каждый класс имеет минимум 100+ изображений
- [ ] ✅ Достаточно места на диске (~2GB для модели и кэша)

## 🆘 Если ничего не помогает:

1. **Переустановить окружение:**

   ```bash
   python -m venv fresh_env
   fresh_env\Scripts\activate  # Windows
   pip install -r requirements.txt
   ```

2. **Использовать Docker (если доступен):**

   ```bash
   docker run -it --gpus all tensorflow/tensorflow:latest-gpu-py3
   ```

3. **Использовать Google Colab:**
   - Загрузить код в Colab
   - Все зависимости уже установлены
   - Бесплатный GPU доступ

## 📞 Получить помощь:

Если проблемы остаются, создайте issue с:

- Версией Python (`python --version`)
- Версией ОС
- Полным текстом ошибки
- Результатом `python check_dependencies.py`
